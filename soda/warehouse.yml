# warehouse.yml - Soda Core data source configuration
data_source youtube_dw:
  type: postgres
  host: ${PGHOST}
  port: ${PGPORT}
  username: ${PGUSER}
  password: ${PGPASSWORD}
  database: ${PGDATABASE}
